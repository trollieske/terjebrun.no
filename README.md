# terjebrun.no

Nettside for **terjebrun.no**.

Status: oppstart — design og innhold ikke bestemt ennå.

## Struktur

```
terjebrun.no/
├── index.html      foreløpig plassholder
├── .gitignore
└── README.md
```

## Kjøre lokalt

Ingen byggesteg ennå. Åpne fila direkte, eller start en enkel server:

```bash
python3 -m http.server 8000    # http://localhost:8000
```

## Videre

- [ ] Avklare stack (ren HTML/CSS eller Astro)
- [ ] Avklare innhold og sider
- [ ] Design: layout, farger, typografi
- [ ] Domene/DNS og deploy (Cloudflare Pages / GitHub Pages / webhotell)

## Deploy

Ikke satt opp ennå. Domenet `terjebrun.no` svarte ikke på DNS-oppslag
sist det ble sjekket — må pekes mot valgt host når vi kommer dit.
